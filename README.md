# Comparison
Utilizing the NLP container this code does not run do to a OOM error. Utilizing the Casestone container, this code takes ~4 minutes to run. 


# Generative AI Disclaimer
Generative AI was used in thie project to port by jupiter notebook python code (which was written by myself) into python code which is split into the for files as following
    (1) preprocessing.py
    (2) model.py
    (3) evaluate.py
    (4) main.py
The AI's was instructed to (1) improve on my code, (2) become PEP8 complient, and (3) use relative file paths. 

